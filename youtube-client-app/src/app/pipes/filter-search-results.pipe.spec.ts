import { FilterSearchResultsPipe } from './filter-search-results.pipe';

describe('FilterSearchResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
